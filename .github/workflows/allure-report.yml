name: Run Selenium Tests and Generate Allure Report

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install allure-pytest

      # 4️⃣ Create required directories
      - name: Create required directories
        run: |
          mkdir -p logs reports screenshots allure-results allure-report

      # 5️⃣ Install utilities (unzip)
      - name: Install unzip
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip

      # 6️⃣ Setup Allure CLI (using pre-downloaded zip)
      - name: Setup Allure CLI
        run: |
          unzip allure-commandline-2.35.1.zip -d ./allure-cli
          chmod +x ./allure-cli/bin/allure
          echo "::add-path::${PWD}/allure-cli/bin"

      # 7️⃣ Run Selenium tests with Allure results
      - name: Run Pytest with Allure
        run: |
          pytest --alluredir=allure-results

      # 8️⃣ Generate Allure HTML report
      - name: Generate Allure HTML Report
        run: |
          ./allure-cli/bin/allure generate allure-results -o allure-report --clean

      # 9️⃣ Upload Allure raw results
      - name: Upload Allure results
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: allure-results

      # 🔟 Upload Allure HTML report
      - name: Upload Allure HTML report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: allure-report

      # 1️⃣1️⃣ Upload logs and screenshots (optional)
      - name: Upload logs and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-artifacts
          path: |
            logs
            reports
            screenshots
